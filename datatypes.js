// Booleans can only have two values: true, false

 var x = 5 == 5;
 console.log(x);

 // arrays; a group of values to a variable

 var foods = ["pizza", "chips", "apple"]
 console.log(foods);
 console.log(foods[0]); // logs the first value

 // objects; written with curly braces, written as name:value pairs

 var pizza = {type:"cheese", price:"£300", yummy:"no"};
console.log(pizza.type);
console.log(pizza);